apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
spec:
  ports:
  - port: {{ .Values.app.servicePort }}
    protocol: TCP
    targetPort: {{ .Values.app.containerPort }}
  selector:
    app: {{ .Values.app.name }}
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.db.name }}
spec:
  ports:
  - port: {{ .Values.db.servicePort }}
    targetPort: {{ .Values.db.name }}-port
    protocol: TCP
  selector:
    app: {{ .Values.db.name }}
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: vprocache01
spec:
  ports:
  - port: {{ .Values.memcached.servicePort }}
    targetPort: {{ .Values.memcached.name }}-port
    protocol: TCP
  selector:
    app: {{ .Values.memcached.name }}
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.rabbitmq.name }}
spec:
  ports:
  - port: {{ .Values.rabbitmq.servicePort }}
    targetPort: {{ .Values.rabbitmq.name }}-port
    protocol: TCP
  selector:
    app: {{ .Values.rabbitmq.name }}
  type: ClusterIP
