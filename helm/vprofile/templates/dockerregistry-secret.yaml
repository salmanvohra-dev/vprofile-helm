{{- if .Values.dockerregistry.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: dockerregistry-secret
type: kubernetes.io/dockercfg
data:
  .dockercfg: {{ printf "{\"auths\":{\"%s\":{\"username\":\"%s\",\"password\":\"%s\",\"email\":\"%s\",\"auth\":\"%s\"}}}" .Values.dockerregistry.server .Values.dockerregistry.username .Values.dockerregistry.password .Values.dockerregistry.email (printf "%s:%s" .Values.dockerregistry.username .Values.dockerregistry.password | b64enc) | b64enc }}
{{- end }}
